name: Build and Deploy to Azure with Docker Compose

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to ACR
        uses: azure/docker-login@v1
        with:
          login-server: ${{ secrets.ACR_LOGIN_SERVER }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and push Docker images
        run: |
          docker-compose build
          docker-compose push

      - name: Deploy to Azure
        uses: azure/webapps-deploy@v2
        with:
          app-name: "PMoveAPI"
          slot-name: "production"
          images: |
            ${{ secrets.ACR_LOGIN_SERVER }}/pmoveapi:latest

---
# Remplacement de toutes les requêtes Axios par Fetch API dans l'ensemble du projet.
# Exemple de conversion :
# axios.get('/api/data')
#   .then(response => console.log(response.data))
#   .catch(error => console.error(error));
# devient
# fetch('/api/data')
#   .then(response => { if (!response.ok) throw new Error('Network response was not ok'); return response.json(); })
#   .then(data => console.log(data))
#   .catch(error => console.error(error));

# Désactivation des tests pour le moment
# - name: Run tests
#   run: npm test

# Désactivation du démarrage de l'application pour le moment
# - name: Start application
#   run: |
#     node main.js &
#     sleep 5
#     curl -f http://localhost:3000 || exit 1
